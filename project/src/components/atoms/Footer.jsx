import styles from "../../styles/footer.module.sass"

export default function Footer () {
    return (
        <footer className={styles.footer}>
            <p>special project by <span>kapustin.team</span></p>
        </footer>
    )
}